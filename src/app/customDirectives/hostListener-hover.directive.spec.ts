import { HostListenerHoverDirective } from './hostListener-hover.directive';

describe('HostListenerHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new HostListenerHoverDirective();
    expect(directive).toBeTruthy();
  });
});
