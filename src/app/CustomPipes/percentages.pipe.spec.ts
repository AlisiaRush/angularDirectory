import { PercentagesPipe } from './percentages.pipe';

describe('PercentagesPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentagesPipe();
    expect(pipe).toBeTruthy();
  });
});
